# Munsy Games — Angular Landing Template

## Setup
1. `npm install`
2. `npm start` (or `ng serve --open`)

This is a minimal modern Angular standalone component project for a gaming studio landing site.

Replace mock data in `src/app/data.service.ts` with your real text/images as needed.
